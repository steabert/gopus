version: "2"
sql:
  - engine: "sqlite"
    queries: "rds/query.sql"
    schema: "rds/schema.sql"
    gen:
      go:
        emit_pointers_for_null_types: true
        package: "rds"
        out: "rds"
